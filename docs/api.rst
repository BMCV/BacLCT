API reference
=============

Generated from docstrings, one page per module. Expanding `baclct` below opens on
`baclct.BacLCT`, the top-level entry point for training and tracking. The submodules
listed further down that page (`features`, `data`, `models`, `tracking`, ...) document
the individual pipeline stages, for readers who build on them directly rather than
through `BacLCT`.

.. autosummary::
   :toctree: _autosummary
   :recursive:

   baclct
