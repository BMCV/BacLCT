"""Hydra config files.

Hydra configs are organized as config groups (e.g., `data`, `model`), global overrides
(e.g., `experiment`), and task- or dataset-specific overrides (e.g.,
`task`, `dataset`). The main training config is defined in `default.yaml`.
"""
