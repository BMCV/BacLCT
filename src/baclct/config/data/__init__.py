"""Configs for single-sequence graph datasets.

Assembles feature configs from `features` (e.g., which handcrafted or deep features
should be used) and defines graph parameters. This config provides default graph params
which generally should be adapted in the specific `dataset` config.
"""
