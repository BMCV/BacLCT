# @package _global_

defaults:
  - override /hydra: no_logs

fold: 100

data:
  batch_size: 1
  batch_size_val: 1

  num_workers: 0
  num_workers_val: 0

trainer:
  devices: 1

  limit_train_batches: 1
  limit_val_batches: 1

  max_epochs: 2
  check_val_every_n_epoch: 1
